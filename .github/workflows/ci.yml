name: CI

on:
  push:
    branches: [main]
  pull_request:

permissions:
  contents: read
  security-events: write   # for SARIF upload

jobs:
  build:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v4

      - name: Set up JDK 21
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: 21
          cache: maven

      - name: Build and test
        run: mvn -B -ntp verify

      - name: Dogfood scan (sprig scans its own sources)
        # Findings (exit 1) are expected and fine — this step's job is only to
        # catch sprig crashing/erroring (exit 2) on a real, if friendly, project.
        # Test fixtures under src/test/resources/fixtures are deliberately
        # vulnerable (that's what the rule tests assert against) and must not
        # be uploaded to code scanning as if they were real findings in this
        # repo — exclude them here, not by disabling the rules that catch them.
        run: |
          set +e
          java -jar target/sprig-0.1.0.jar scan . --output sarif --output-file sprig.sarif \
            --exclude-path 'target/**' --exclude-path '**/fixtures/**'
          code=$?
          set -e
          if [ "$code" -eq 2 ]; then
            echo "::error::sprig exited 2 (operational error) while scanning its own source tree"
            exit 1
          fi

      - name: Upload SARIF to code scanning
        if: always()
        uses: github/codeql-action/upload-sarif@v3
        with:
          sarif_file: sprig.sarif
